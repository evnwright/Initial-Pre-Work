// I learned how it's possible to avoid if/else statements when dealing with Boolean values

function isLess(a, b) {
  // Fix this code
  return a <= b;
}

// Change these values to test
isLess(10, 15);
